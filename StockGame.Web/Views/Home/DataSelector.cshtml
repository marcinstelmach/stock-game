@model SelectorDataViewModel
<div class="row text-left">
    <div class="col-6">
        <form asp-controller="Home" asp-action="Results" method="post">
            <div class="form-group row">
                <label for="dataSelector" class="col-sm-3 col-form-label">Select Data</label>
                <div class="col-sm-9">
                    <select asp-for="DataType" asp-items="Model.DataTypes" class="form-control" id="dataSelector"></select>
                </div>
            </div>
            <div class="form-group row">
                <label for="from" class="col-sm-3 col-form-label">From</label>
                <div class="col-sm-9">
                    <input asp-for="From" class="form-control" id="from" type="date" value="@DateTime.Now.AddYears(-1)">
                </div>
            </div>
            <div class="form-group row">
                <label for="To" class="col-sm-3 col-form-label">To</label>
                <div class="col-sm-9">
                    <input asp-for="To" class="form-control" id="To" type="date" value="@DateTime.Now">
                </div>
            </div>
            <div class="form-group row">
                <label for="startAmount" class="col-sm-3 col-form-label">Start amount</label>
                <div class="col-sm-9">
                    <input asp-for="StartAmount" class="form-control" id="startAmount" type="number" value="0">
                </div>
            </div>

            <input type="submit" class="btn btn-danger" value="Search" />
        </form>
    </div>
    <div class="col-5 offset-1">
        <p>Select data source, o which you will work, like ExchangeRate. 
            Remember to select Date from and date to. Dates has to be in the past. The max range between dates is 93 days. 
            If you will select bigger period, it will be trim automatically.</p>
        
        <p>If you want to play a game, put some amount also. It will tell you how much would you earn or lost after period.</p>
    </div>
</div>